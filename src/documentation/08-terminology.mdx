# Terminology

### Story

In the context of `bookemoji`, a story is a svelte file representing documentation, or a set of variants.

### Variant

A variant is a linkable configuration of a Story

### Component Workshop

A component workshop is an application used to build component. They are useful because many stakeholders can interact with it, as well as allow cross-team common-language when describing user interfaces.
